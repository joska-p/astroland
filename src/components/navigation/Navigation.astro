---
import Link from "./Link.astro"

interface Props {
  links: {
    name: string
    href: string
  }[]
}
const { links } = Astro.props
---

<nav class="flex flex-row gap-4">
  {links.map((link) => <Link href={link.href} name={link.name} />)}
</nav>
