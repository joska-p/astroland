---
import type { ImageMetadata } from "astro"
import { Picture } from "astro:assets"

interface Props {
  painting: {
    id: string
    title: string
    filename: ImageMetadata
    year: number
    width: number
    height: number
  }
}

const { painting } = Astro.props
const { id, title, year, width, height, filename } = painting
---

<article
  id={id}
  class="grid justify-items-center md:grid-cols-2 md:items-center snap-start mb-[100vh]"
>
  <Picture src={filename} formats={["avif", "webp"]} alt={title} />
  <header class="font-raleway mt-2 md:mt-0">
    <h2 class="text-2xl font-bold text-center md:mb-8 md:text-4xl">{title}</h2>
    <p class="flex flex-row gap-2">
      <span class="tracking-widest text-right text-amber-900 basis-full">Année:</span>
      <span class="text-left basis-full"
        ><time datetime={String(year)}>{year === 0 ? "NC" : year}</time></span
      >
    </p>
    <p class="flex flex-row gap-2">
      <span class="tracking-widest text-right text-amber-900 basis-full">Hauteur:</span>
      <span class="text-left basis-full">{height === 0 ? "NC" : height + " cm"}</span>
    </p>
    <p class="flex flex-row gap-2">
      <span class="tracking-widest text-right text-amber-900 basis-full">Largeur:</span>
      <span class="text-left basis-full">{width === 0 ? "NC" : width + " cm"}</span>
    </p>
  </header>
</article>
